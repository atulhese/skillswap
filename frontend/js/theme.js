(() => { const root=document.documentElement; const key='skillswap-theme'; const stored=localStorage.getItem(key); const apply=theme=>{root.classList.toggle('dark',theme==='dark'); document.querySelectorAll('[data-theme-toggle]').forEach(button=>{button.setAttribute('aria-label',theme==='dark'?'Switch to light mode':'Switch to dark mode'); button.textContent=theme==='dark'?'Light mode':'Dark mode';});}; apply(stored||'light'); document.addEventListener('click',event=>{const button=event.target.closest('[data-theme-toggle]'); if(!button)return; const next=root.classList.contains('dark')?'light':'dark'; localStorage.setItem(key,next); apply(next);}); const hash=location.hash; if(hash==='#register'&&typeof switchAuthTab==='function') switchAuthTab('register'); if(hash==='#login'&&typeof switchAuthTab==='function') switchAuthTab('login'); if(hash) setTimeout(()=>document.querySelector(hash)?.scrollIntoView({behavior:'smooth',block:'center'}),100); })();
